# =============================================================================
# ShopHub - Django + DRF project dependencies
# =============================================================================
# Django is a "batteries-included" framework (unlike Flask's micro-framework):
# it ships an ORM, admin, middleware, auth, migrations, and more out of the box.
# We add DRF (Django REST Framework) for building the pure-backend API.

# ---- Core framework ------------------------------------------------------
Django==5.2.17              # the framework (incl. ORM, admin, auth, migrations)
djangorestframework==3.15.2 # REST API framework on top of Django (DRF)
psycopg2-binary==2.9.9      # Postgres driver
django-filter==26.1         # declarative filtering for DRF (?category=...) 

# ---- Auth / tokens ---------------------------------------------------------
# DRF ships TokenAuthentication; djangorestframework-simplejwt adds JWT.
djangorestframework-simplejwt==5.3.1

# ---- CORS (browser front-ends calling the API) ----------------------------
django-cors-headers==4.3.1

# ---- Caching / background tasks -------------------------------------------
redis==5.0.4                # Redis client
celery==5.4.0               # background task queue (broker = Redis)
django-celery-beat==2.6.0   # periodic/scheduled tasks via Django admin
django-timezone-field==7.2.2 # (dependency of django-celery-beat)

# ---- Config / env ----------------------------------------------------------
python-dotenv==1.0.1        # load settings from .env

# ---- OpenAPI / docs (batteries make documentation easy too) ---------------
drf-spectacular==0.27.2     # auto-generate OpenAPI schema + Swagger UI

# ---- Testing ---------------------------------------------------------------
pytest==8.2.2
pytest-django==4.8.0        # pytest integration with Django
